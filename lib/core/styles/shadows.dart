import 'package:flutter/material.dart';

class AppShadows {
  static List<BoxShadow> categoriesContainerShadow = [
    BoxShadow(
      color: Colors.black.withOpacity(0.3),
      blurRadius: 3,
      offset: const Offset(0, 3),
    )
  ];
}
